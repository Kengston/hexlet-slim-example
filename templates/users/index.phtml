<form method="GET" action="/users">
    <input type="text" name="search" placeholder="Поиск по имени" value="<?= $searchTerm ?>">
    <button type="submit">Искать</button>
</form>

<table>
    <?php foreach ($users as $user): ?>
        <tr>
            <td>● <?= $user['name'] ?></td>
        </tr>
    <?php endforeach ?>
</table>
